# Fleet learnings

- (2026-07-13, captain, CRITICAL QUALITY FAILURE + BINDING) k-zero visual quality REJECTED by captain despite crew "YES SHIP"/asset-audit claims: ships "polygate" (random polygon glitches - prime suspect: the meshopt force-recompress/quantization pass from the 8-ships review corrupting geometry/normals, plus LOD popping), floor pickups/drops are NOT Tripo-generated at all, many assets non-generated, overall look "like an old Amiga", not AAA 1080p/4K. LESSONS (binding): (1) crew visual-quality and asset-provenance claims are UNRELIABLE without evidence - every asset/visual claim must ship with committed high-res screenshots (1080p+) per asset category, same evidence doctrine as piloting; (2) meshopt/quantization settings must be verified visually against source GLBs after every compression pass - budget compliance never justifies corrupted geometry; (3) the bar is "push the boundaries" AAA at 1080p/4K - renderer quality levers (DPR, AA, tone mapping, shadows, post-processing, texture res, lighting) are in scope, not just assets. Rework flow: evidence-first visual audit scout -> paired co-draft AAA overhaul plan -> overhaul lanes. HARDENED same day (captain): the ENTIRE game must be fully 3D rendered with proper generated assets - NO visible primitive/procedural/low-poly stand-in geometry AT ALL, every visible asset category (ships, pickups/drops, pads, projectiles, props, structures, surfaces, skybox) is a real Tripo-generated 3D asset (API primary, Tripo web fallback); this supersedes the earlier "procedural where generation can't beat it" allowance for anything player-visible. REFERENCE CLASS (captain): Forza Motorsport / GTA 5 / Spider-Man - really high fidelity, full 3D, "everything is just beautiful and stunning"; k-zero's visual work is measured against that bar (within browser limits, pushed to the boundary; more captain reference examples may follow). CONTROLS (captain, 2026-07-13): current steering is "too finicky - too responsive in terms of left and right"; control smoothing/feel (input filtering, steering curves, response tuning) is a first-class transformation workstream, verified by piloted feel-rating. Captain ordered a FULL TRANSFORMATION PLAN (paired co-draft) instead of backlog items - the existing queued k-zero roadmap (AI-combat, multiplayer, production, release, smooth-gate, N0 fixes) gets reconciled by that plan. Plan must also evaluate engine alternatives (Babylon.js, PlayCanvas vs current three.js/R3F) for graphics ceiling + performance with one migration-vs-stay recommendation (captain, same day). SUPERSEDED hours later by captain PLATFORM PIVOT: k-zero becomes a NATIVE iOS/iPadOS APP on the UNITY ENGINE ("the web app... has been ridiculous"); web engine comparisons are moot - the plan now covers Unity iOS execution: URP-vs-HDRP choice for mobile fidelity, Metal/device tiers/ProMotion, TS-sim -> C# port strategy, Tripo->Unity asset pipeline (raw 4k sources reusable), touch+gamepad controls (fixing the finicky steering in the new input system), SpacetimeDB C# SDK for multiplayer, Unity toolchain/CI setup, App Store distribution, and the fate of the web build. bootstrap-ios skill is installed for Apple work.

- (2026-07-13) Validation dashboard tooling (lives in `bin/`, synced into k-code): `bin/fm-validation-dashboard.sh` is a live TUI of every in-flight task + its no-mistakes state; `bin/fm-dashboard-launch.sh` auto-spawns it. Two hard-won rendering rules: (1) FLICKER-FREE = compose the whole frame into a buffer, then paint with cursor-home `\033[H` + `\033[0J` clear-to-end and width-padded lines - NEVER a full-screen `\033[2J` per frame (that blanks then slow-redraws = flicker). (2) Pace with `sleep`, never a `read -t` timeout (read spins instantly on a non-TTY = busy-loop). Reads use `fm-crew-state.sh` per task for ACCURACY (the status-log tail shows the last event e.g. "done: committed" while the crew is actually validating - accuracy is the whole point). herdr facts: to show a pane in the left AGENTS sidebar it must be a herdr agent - `herdr tab create --no-focus --label X` to get a tab_id, then `herdr agent start X --no-focus --tab <tab_id> -- bash <script>`; `agent start` WITHOUT --tab lands in the current tab (splits under the captain's main pane). Send commands to a pane with `herdr pane send-text <pane> "<cmd>"` then `herdr pane send-keys <pane> Enter`.
- (2026-07-13, corrected 2026-07-14) k-code (`korallis/k-code`) is the captain's canonical Firstmate operating-home fork: mirrored Firstmate material, public-safe `config/` routing, durable `data/` memory, dashboard additions, and the complete reviewed skill snapshot. Product repositories are local runtime inputs only: `projects/` is ignored, no product path or gitlink is tracked, and no `.gitmodules` state exists. `bin/kcode-sync.sh` mirrors the live working tree while protecting fork-owned README, CI, art, skill inventory, and packaging surfaces; it validates every active skill root and removes stale product index entries without deleting local ignored checkouts. k-code keeps focused fork-integrity CI instead of upstream Firstmate's development workflows.
- (2026-07-13) computer-use (screenshot/control the captain's terminal) needs macOS Screen Recording permission - a system dialog appears on first `request_access`; the captain must grant it in System Settings before screenshots work. Terminals (Ghostty) are "click" tier: visible + clickable, but no typing (use Bash/herdr for input).
- (2026-07-13) Supervision false-alarm pattern (recurring, benign - do NOT chase as wedges): a grok crewmate driving its own no-mistakes validation shows a QUIET/idle pane between pipeline step boundaries, which trips `stale: <window> (possible wedge, escalation 1)` watcher wakes even though the run is healthy. The authoritative signal is `bin/fm-crew-state.sh <id>` -> `working · run-step · validating (running) · status-log superseded by active run`, or `no-mistakes axi status` in the worktree showing `active_steps` with a recent `last_activity` (seconds/1-2 min ago). Handle each: read run-step, confirm alive, re-arm. Only inspect the pane deeply when escalation count reaches 2+ or the reason carries `demand-deep-inspection`. Cosmetic companion noise: `bin/backends/herdr.sh: line 112x: printf: write error: Broken pipe` on watcher-arm output is harmless - ignore it.
- (2026-07-13) A mid-session `/model` switch (observed switching primary Fable 5 -> Opus 4.8 1M during a heavy multi-lane session) can leave the tracked background watcher stale: the beacon froze ~98 min while conversational re-arms still reported "watcher: started ... beacon fresh", and `bin/fm-guard.sh` (invoked by `bin/fm-wake-drain.sh`) surfaced it as `WATCHER DOWN ... last beat 5800s+ ago`. The turn-end guard + the drain-time beacon check are the backstops that catch this. Fix is the normal one - re-run `bin/fm-watch-arm.sh` as its own background task and confirm a genuinely fresh beacon via `stat state/.last-watcher-beat`. Lesson: after any harness/model switch mid-flight with tasks live, explicitly re-arm and verify the beacon timestamp rather than trusting an "attached/started" line alone.
- (2026-07-13) A crewmate's `done: PR <url> checks green` can PRECEDE it pushing later commits to that PR: a subsequent no-mistakes gate round (evidence/harness/fix commits) lands after the first checks-green report. Merging on that first green squash-merges an incomplete PR AND leaves the worktree holding unlanded commits, so `bin/fm-teardown.sh` correctly REFUSES (its landed-work guard is the backstop that caught it). Recovery: have the crew rebase the residual commits onto merged main and open a follow-up PR - this happened with kz-funplay (PR 24 merged early -> evidence follow-up PR 25). Lesson: for a task still inside an active validation run, prefer confirming the run reached a terminal `outcome:` (via `bin/fm-crew-state.sh` = `run passed`/`checks-passed`, not just an interim checks-green status line) before merging, or expect and manage a follow-up PR.

- (2026-07-13, captain, BINDING) k-zero assets are GENERATION-FIRST: every game asset (ships, environment/set-dressing, props, weapon/projectile/pickup models, structures) should be built via the Tripo 3D generation pipeline (threejs-3d-generator; API primary, 25k credits funded; website fallback) for a graphically stunning, cohesive AAA look - procedural/CC0 only where generation genuinely can't beat it, recorded with reason in the provenance manifest. BUT "runs really well" is co-equal and non-negotiable: every generated asset goes through meshopt optimize + LOD chains and MUST hold the Tier-A budget (<=180 draws / <=900k tris / <=256 MB textures, 60fps). Stunning AND performant, verified by piloted play. An asset-completeness check (nothing shipping as a grey placeholder where a real model belongs) is part of the fun/QA gate.
- (2026-07-13, captain, BINDING - RAISED same day) k-zero quality bar: beyond AAA+FUN, the game must be COMMERCIALLY SMOOTH - "buttery smooth... a game people would buy": frame-time consistency (no hitches/stutter, not just 60fps average), stable (no crashes, extended-soak crash-free), zero glitches, controls easy and smooth, and NEVER fall through the floor (regression-stress the fix-fallthrough soak). Smoothness/stability verification is a first-class gate alongside fun and effects: piloted playtests must explicitly rate frame pacing, control feel, and stability, and any hitch/glitch/crash blocks done. ALSO UNIVERSAL (captain, same message): real-user testing (the BRB-style skills) is part of the standard workflow for EVERYTHING, not just k-zero - every project's runnable ship work gets user-tested for smooth, stable, glitch-free behavior before done.
- (2026-07-13, captain, BINDING) k-zero quality bar = "AAA and genuinely FUN to play", not just functionally correct. Every gameplay/polish brief must treat these as first-class, verified-by-piloting acceptance criteria: all EFFECTS actually fire and read at speed (boost VFX/exhaust, weapon projectile trails + muzzle + impact, shield bubble/hit flash, countermeasure/EMP, destruction burst, respawn shimmer); all HUD elements animate correctly (energy/boost bar drain+refill, fatal-boost warning, item-slot fills, absorb ring, lap/position/SLA timers, countdown); track-SURFACE life (boost pads/speed strips visibly on the floor, surface detail, hazards/bumps, pickup pads glowing); and full generated-asset rendering (Tripo API primary - 25k credits funded; website fallback). FUN is an explicit gate, not a nice-to-have: a running-bug-review-board playtest that actually drives the ship and rates game-feel (speed sensation, cornering, combat impact, juice/hitstop/shake) - a not-fun or dead-effects verdict blocks done. Assets must never ship dark/inert.
- (2026-07-13, captain, BINDING - TIGHTENED same day after captain observed crews rendering without touching controls) k-zero piloting claims require EVIDENCE ARTIFACTS, not assertions: every piloted-QA pass must commit/attach proof of real control use - the exact input sequence driven (keys or __KZERO_TEST__ script), before/after telemetry deltas (position/heading/speed per maneuver: accelerate, corner L/R, boost, fire, sideshift), and ideally a recorded GIF/video of the run. A "piloted" claim with no evidence is treated as NOT piloted and the QA pass is rejected. Reaffirmed same message: AAA paid-game polish is the bar, ALL assets go through the Tripo 3D generation pipeline, and zero bugs is critical.
- (2026-07-13, captain, BINDING) k-zero browser verification MUST actually PILOT the ship, not just load the scene. Loading + rendering is not gameplay QA - it misses inverted/dead controls, broken thrust/steering, weapons that won't fire, AI that won't race. Every k-zero ship/scout brief's acceptance criteria must require driving with real inputs and OBSERVING the result: either (a) real keyboard input via chrome-devtools-axi (accelerate, steer L/R, boost, fire, sideshift) with before/after observation of ship position/heading/speed, or (b) the deterministic __KZERO_TEST__ input hooks / line-bot driving the input ring buffer through a real maneuver, asserting the craft actually moved/turned/cornered/fired. "Scene renders" alone is a REJECTED verification. The running-bug-review-board live-QA pass (already standing practice) satisfies this when it drives inputs; name it explicitly in gameplay-touching briefs.

- (2026-07-13) grok 0.2.99 removed `--reasoning-effort xhigh` (accepts only high/medium/low; harness-adapters' table was verified on 0.2.73). Grok's effort ceiling is now `high` - crew-dispatch.json updated accordingly, and captain.md's "grok xhigh ceiling" note is stale. The harness-adapters skill table needs a PR-shipped fix (queue a firstmate-repo task when the fleet quiets).

- (2026-07-13) Project clones must use SSH origin remotes (`git@github.com:korallis/<repo>.git`). The k-zero clone was initially HTTPS and broke `treehouse get` with password prompts (git had no credential helper; gh's protocol is ssh). SSH auth is set up machine-wide via the 1Password-managed key, so clone new projects with SSH URLs.

- (2026-07-13) GitHub SSH auth for this machine ("M5") uses a 1Password-managed ed25519 key: vault item "GitHub SSH Key (M5)" (id `o2tqagljyi7hrlmipge4ciaixu`) in the Dev-Env vault, installed at `~/.ssh/id_ed25519`, registered on GitHub (account `korallis`) as "M5 (1Password)". Recover after machine loss with `op read "op://Dev-Env/o2tqagljyi7hrlmipge4ciaixu/private key?ssh-format=openssh"`. Old passphrase-protected key backed up at `~/.ssh/id_ed25519.bak-m5-passphrase`; its stale GitHub entry "M5" (added 2026-07-12) can be deleted once confirmed unused.
- (2026-07-13) 1Password CLI (`op` 2.34.x) is installed and app-integrated; secret references reject parentheses in item titles - use the item ID in `op://` paths.
- (2026-07-13) Muve/service-referral binding business rules (captain): children are taken under TDDI - Ofsted is NOT required, remove Ofsted machinery from all service-referral work; staffing ratios are fixed hours (1:1=168 h/wk, 2:1=336, 3:1=504); pricing is cost-to-LA with flat charge-out rates and NO additional cost lines (HCA £32/h, RMN £65/h; rates belong in an admin-editable table). Recorded in data/sr-plan.md section 4b. EXTENDED by captain P2 answers 2026-07-13 (full list: converged-plan.md section 0): everything under CQC, TDDI is the basis, in-product Approver role is the sign-off/approval authority (no external clinical gate); nursing default one full RMN post inside the ratio but an override allows finer HCA/RMN splits (totals stay fixed); reviewers MAY reprice a different staffing basis via the chatbot (versioned, decision-bindable revisions required); intake supports BOTH multi-file first-contact and amendments, each queuing exactly one job; nothing is out of scope for referrals; "marked sent" = user tickbox writing an append-only claim.
- (2026-07-13) k-zero multiplayer hosts on SpacetimeDB Cloud - the captain already has an active subscription; never plan self-hosting for it. Music for k-zero: license-free (CC0/CC-BY) preferred over commissioning.
- (2026-07-13) `running-bug-review-board` (RayFernando1337/rayfernando-skills) is installed on all three harnesses: a live real-user browser QA pass (P0/P1/P2 bug board, ship/no-ship verdict) that complements no-mistakes' static review + tests. STANDING PRACTICE (captain, 2026-07-13, UNIVERSAL - ALL projects, not just k-zero): every ship brief with a runnable surface mandates a running-bug-review-board (BRB) real-user QA pass on the RUNNING app before reporting done - drive it like an actual user (click, type, navigate, submit the real flows), not just load/render or unit tests. A NO-SHIP / P0-open verdict blocks done. This is in ADDITION to no-mistakes (no-mistakes gates the code; BRB gates the running product); no-mistakes has no hook to call other skills, so firstmate wires BRB in at the brief layer for both apps. Name the BRB pass explicitly in every runnable ship brief's acceptance criteria. The skill is installed for claude/codex/grok so any crew can run it. Pure-content/config/docs tasks with no runtime surface are the only exemption. `bootstrap-ios` from the same repo is installed for future Apple work (bikers-club). The repo's `waves`/`waves-codex` orchestration skills were deliberately NOT installed - Cursor-specific / overlapping with firstmate's own fleet orchestration.
- (2026-07-13) Asset-generation keys in 1Password Dev-Env: "Tripo API Key" (`h4vrivdhvlrkjmwgnpacbwko6i`, env `TRIPO_API_KEY`) and "ElevenLabs API Key" (`egkzfaihhwzbntnahiis5sw3a4`, env `ELEVENLABS_API_KEY`). `GEMINI_API_KEY` (threejs-image-generator) is still unconfigured - Grok /imagine is the 2D generation route. Crewmate briefs needing generation should export the vars via `op read "op://Dev-Env/<item-id>/credential"` in their worktree, never bake keys into files.
- (2026-07-13) Exa and Ref MCP servers are wired into every active harness - claude (`~/.claude.json`: header auth), grok (`~/.grok/config.toml`), codex (`~/.codex/config.toml`), and Pi (captain-confirmed, same availability contract) - and must be used by every research/planning voice regardless of whether Pi is running an OpenAI or xAI model. Grok's MCP client ignores custom headers for auth, so grok and codex use the query-param URL forms (`mcp.exa.ai/mcp?exaApiKey=`, `api.plan.ref.tools/mcp?apiKey=`); claude uses headers (`x-api-key` / `x-ref-api-key`). Keys live in 1Password Dev-Env: "Exa API Key" (`4m5524am3tg5ljcr7dnxqlsjoq`), "Ref API Key" (`eg5howgk3ugaqdujh3sl7p7nmq`).
- (2026-07-13) The threejs-game-skills pack (github.com/majidmanzarpour/threejs-game-skills) is installed user-level for claude (`~/.claude/skills`) and codex (`~/.codex/skills`): entry point is `threejs-game-director`, which routes to the specialist gameplay/graphics/UI/asset/audio/debug/QA skills. Also installed for grok in `~/.grok/skills/` (grok reads the same SKILL.md format; verified discovered via `grok inspect`), so all three crew harnesses can use `threejs-game-director` for k-zero work. The emilkowalski/skills pack (improve-animations, review-animations, animation-vocabulary, apple-design, emil-design-eng) is likewise installed on all three harnesses - use them in any brief that creates or polishes UI/HUD/animation work.
- (2026-07-13) Project env files live in 1Password Dev-Env vault as documents named `env/<project>/<envfile>` (plus a `restore-envs.sh` helper doc). k-zero's is `env/k-zero/.env.local` (id `f6ruphed7dg363uj4676muyhxe`), currently holding `VERCEL_OIDC_TOKEN` only. A task needing it fetches into its own worktree with `op document get f6ruphed7dg363uj4676muyhxe --out-file .env.local`; never write env files into `projects/` clones. Note: Vercel OIDC tokens are short-lived (~12h) - if the app rejects it, refresh via `vercel env pull` and update the vault doc.
- (2026-07-13, captain-flagged as KEY) service-referral prod migrations are APPLIED AUTOMATICALLY ON DEPLOY and must be verified after every migration-bearing merge. Mechanism: `package.json` build runs `node scripts/db/migrate-on-deploy.mjs` as its FIRST step (before typecheck/next build), which applies all pending migrations from BOTH sets - `migrations/` (tracked in `schema_migrations`) and `db/migrations/` (tracked in `_migrations`) per `scripts/db/migration-sets.mjs` - against `DATABASE_URL_UNPOOLED`. It FAILS the build (exit 1) on a Vercel prod/preview build if that env is unset, so a schema-lagging deploy can never silently ship (this is the recurrence-fix from the `evaluation_runs`/0008 outage, PR #25). Preview AND production builds run it against the SAME shared Neon DB, so an additive migration is often already applied by a branch's preview build before its prod deploy (M4's `0014_charge_out_rate_cards` showed `skip (already applied)` on the main deploy - it landed via the fm/sr-m4-a5 preview first). VERIFICATION STEP (do this on every SR merge that carries a migration): after merge, confirm the production deployment for that commit is `state: READY, target: production` via the Vercel MCP (team `team_knUcJjrcRt5oxglE6c3E3zTR`, project `prj_KjAkuHKKJtd06DNIMHutagOcYbNF` = service-referral), and read the build-log head for the `migrate-on-deploy: applying…` / `apply|skip 00NN_*.sql` / `migrate-on-deploy: done` lines. A READY production deploy == migrations applied (migrate runs first and fails closed). An ERROR/`missing DATABASE_URL_UNPOOLED` deploy is a prod-blocking escalation. Migrations 0012 (M2) and 0013 (M3) land the same way when those milestones deploy.
